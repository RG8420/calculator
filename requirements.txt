# Calculator package dependencies
# The calculator itself uses only Python standard library.
# These are for development and testing only.

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Development
setuptools>=68.0.0
wheel>=0.40.0